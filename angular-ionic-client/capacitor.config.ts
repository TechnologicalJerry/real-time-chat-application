import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'real-time-chat-application',
  webDir: 'www'
};

export default config;
